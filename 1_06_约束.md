[toc]

# 1、什么是约束

约束（`Constraint`）是数据库中用来强制数据完整性的规则。它规定了表中数据的取值、关系和行为，防止无效或不一致的数据进入数据库。例如：

- 这个字段不能为空（`NOT NULL`）
- 这个字段的值必须唯一（`UNIQUE`）
- 这个字段的值必须在某个范围内（`CHECK`）
- 这张表和另一张表的数据必须一一对应（`FOREIGN KEY`）

数据完整性（`Data Integrity`）是指数据的精确性（`Accuracy`）和可靠性（`Reliability`）。它是防止数据库中存在不符合语义规定的数据和防止因错误信息的输入输出造成无效操作或错误信息而提出的。

为了保证数据的完整性，`SQL` 规范以约束的方式对表数据进行额外的条件限制。从以下四个方面考虑：

- 实体完整性（`Entity Integrity`） ：例如，同一个表中，不能存在两条完全相同无法区分的记录;
- 域完整性（`Domain Integrity`） ：例如：年龄范围0-120，性别范围“男/女”
- 引用完整性（`Referential Integrity`） ：例如：员工所在部门，在部门表中要能找到这个部门
- 用户自定义完整性（`User-defined Integrity`） ：例如：用户名唯一、密码不能为空等，本部门经理的工资不得高于本部门职工的平均工资的5倍。

约束是表级的强制规定。

可以在创建表时规定约束（通过 `CREATE TABLE` 语句），或者在表创建之后通过 `ALTER TABLE` 语句规定约束。

# 2、约束的分类

1. 根据约束数据列的限制，约束可分为：
   - 单列约束：每个约束只约束一列
   - 多列约束：每个约束可约束多列数据
2. 根据约束的作用范围，约束可分为:
   - 列级约束：只能作用在一个列上，跟在列的定义后面
   - 表级约束：可以作用在多个列上，不与列一起，而是单独定义
3. 根据约束起的作用，约束可分为:
   - `NOT NULL` 非空约束，规定某个字段不能为空
   - `UNIQUE` 唯一约束，规定某个字段在整个表中是唯一的
   - `PRIMARY KEY` 主键(非空且唯一)约束
   - `FOREIGN KEY` 外键约束
   - `CHECK` 检查约束
   - `DEFAULT` 默认值约束

> 测试数据准备

创建两个数据库 `test_constraints_1` 和 `test_constraints_2`，在两个数据库下，创建一张 `user` 表;

```sql
# 创建数据库
create database test_constraints_1;
create database test_constraints_2;

# 创建表
create table if not exists user(id int primary key auto_increment, name varchar(20));
```

数据库表的约束信息存储在 `information_schema.table_constraints` 表中，可以通过如下方式，查询数据库中 某张表的 所有约束信息。

```sql
# information_schema数据库名（系统库）
# table_constraints表名称（专门存储各个表的约束）
SELECT * FROM information_schema.table_constraints WHERE table_name = '表名称';
```

注意:

1. 数据库（`Database/Schema`）提供了逻辑隔离。表名只在所属的数据库内唯一，跨数据库重名是完全允许的。
2. 当查询 `information_schema` 时，如果没有指定 `TABLE_SCHEMA`，`MySQL` 会返回所有数据库中满足条件的记录。

查询 `user` 表的约束信息

```sql
# 查询 user 表的约束信息，可以看到，会返回所有数据库中的 user 表约束信息
mysql> select * from information_schema.table_constraints where table_name = 'user';
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA  | CONSTRAINT_NAME | TABLE_SCHEMA       | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| def                | mysql              | PRIMARY         | mysql              | user       | PRIMARY KEY     | YES      |
| def                | test_constraints_1 | PRIMARY         | test_constraints_1 | user       | PRIMARY KEY     | YES      |
| def                | test_constraints_2 | PRIMARY         | test_constraints_2 | user       | PRIMARY KEY     | YES      |
| def                | testdb             | PRIMARY         | testdb             | user       | PRIMARY KEY     | YES      |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
4 rows in set (0.01 sec)

# 查询 test_constraints_1 库下的 user 表的约束信息
mysql> select * from information_schema.table_constraints where table_schema = 'test_constraints_1' and table_name = 'user';
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA  | CONSTRAINT_NAME | TABLE_SCHEMA       | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| def                | test_constraints_1 | PRIMARY         | test_constraints_1 | user       | PRIMARY KEY     | YES      |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
1 row in set (0.00 sec)

mysql>
```

# 3、非空约束 `NOT NULL`

非空约束 `NOT NULL` 限制某个字段/某一列的值不允许为空。

- `mysql` 默认所有的类型的值都可以是 `NULL`，包括`INT`、`FLOAT`等数据类型；
- 非空约束只能出现在表对象的列上，只能某个列单独限定非空，不能组合非空；
- 一个表可以有很多列都分别限定了非空；
- 空字符串 `''` 不等于 `NULL`，`0` 也不等于 `NULL`;

注意:

`information_schema.TABLE_CONSTRAINTS` 不会返回非空（`NOT NULL`）约束。这是因为 `MySQL` 将 `NOT NULL` 视为列属性而非独立的约束对象，因此它不会出现在 `TABLE_CONSTRAINTS` 视图中。

## 3.1、添加非空约束 (建表时)

```sql
CREATE TABLE 表名称(
字段名  数据类型,
字段名  数据类型 NOT NULL,
字段名  数据类型 NOT NULL
);
```

> 创建 `emp` 表

- `primary key` 主键默认非空
- `name` 属性添加了 非空约束
- `age` 属性没有添加非空约束，可以写入 `null` 值;

```sql
mysql> create table emp(id int primary key auto_increment, name varchar(20) not null, age int);
Query OK, 0 rows affected (0.06 sec)

mysql> desc emp;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| name  | varchar(20) | NO   |     | NULL    |                |
| age   | int         | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
3 rows in set (0.00 sec)

mysql>

# 插入数据成功
mysql> insert into emp values(1, 'zhangsan', null);
Query OK, 1 row affected (0.02 sec)

# name 传 null 时，insert 报错
mysql> insert into emp values(2, null, null);
ERROR 1048 (23000): Column 'name' cannot be null

# 此时只有一条数据
mysql> select * from emp;
+----+----------+------+
| id | name     | age  |
+----+----------+------+
|  1 | zhangsan | NULL |
+----+----------+------+
1 row in set (0.00 sec)

mysql>
```

## 3.2、添加非空约束 (建表后)

```sql
alter table 表名称 modify 字段名 数据类型 not null;
```

> 修改 `emp.age` 字段非空

```sql
# 直接修改时，由于数据库中已经存在了 null 值，所以会报错
mysql> alter table emp modify age int not null;
ERROR 1138 (22004): Invalid use of NULL value
mysql>
```

解决方案:

```sql
# step1: 处理历史数据中  age = null 的数据
mysql> update emp set age = 0 where age is null;
Query OK, 1 row affected (0.02 sec)
Rows matched: 1  Changed: 1  Warnings: 0
# step2: 设置 age 列非空
mysql> alter table emp modify age int not null;
Query OK, 0 rows affected (0.14 sec)
Records: 0  Duplicates: 0  Warnings: 0
# 再次查看 emp 表结构，可以看到 age 列非空约束
mysql> desc emp;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| name  | varchar(20) | NO   |     | NULL    |                |
| age   | int         | NO   |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
3 rows in set (0.00 sec)

mysql>
```

## 3.3、删除非空约束

```sql
# 指定数据列可以为null
alter table 表名称 modify 字段名 数据类型 NULL;

# 去掉 not null，相当于修改某个非注解字段，该字段允许为空
alter table 表名称 modify 字段名 数据类型;
```

将 `emp.age` 重新设置为可空

```sql
mysql> alter table emp modify age int null;
Query OK, 0 rows affected (0.14 sec)
Records: 0  Duplicates: 0  Warnings: 0

mysql> desc emp;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| name  | varchar(20) | NO   |     | NULL    |                |
| age   | int         | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
3 rows in set (0.00 sec)

mysql>
```

# 4、唯一性约束 `UNIQUE`

唯一性约束（`UNIQUE`）是数据库中用于确保 **某一列或多列的组合值** 在整个表中不重复的一种约束规则。

- 同一个表可以有多个唯一约束。
- 唯一约束可以是某一个列的值唯一，也可以多个列组合的值唯一。
- 唯一性约束允许列值为空，数据可以出现多个空值。
- 在创建唯一约束的时候，如果不给唯一约束命名，就默认和列名相同。
- `MySQL`会给唯一约束的列上默认创建一个唯一索引。


> 唯一约束自动创建索引

在 `MySQ`L 中，当为列定义 `UNIQUE` 约束时，系统会自动在该列上创建一个唯一索引（`B-Tree` 结构）。这个索引不仅能保证值的唯一性，还能加速基于该列的查询（等值查询、范围查询、排序等）。

```sql
# step1: 创建 user 表
mysql> create table if not exists user(id int, name varchar(20), cert_no varchar(20) not null default '');
Query OK, 0 rows affected (0.03 sec)

# step2: 查看 user 表的索引，此时没有索引
mysql> show index from user\G;
Empty set (0.00 sec)

# step3: 为 user.cert_no 字段添加唯一约束
mysql> alter table user add constraint cert_no unique key (cert_no);
Query OK, 0 rows affected (0.04 sec)
Records: 0  Duplicates: 0  Warnings: 0

# step4: 再次查看 user 表的索引
# \G 表示垂直显示
mysql> show index from user\G;
*************************** 1. row ***************************
        Table: user        # 表名
   Non_unique: 0           # 0 表示唯一索引，1 表示非唯一
     Key_name: cert_no     # 索引名称
 Seq_in_index: 1           # 索引中列的顺序
  Column_name: cert_no     # 列名
    Collation: A           # 排序方式 A:升序 D:降序 NULL:未排序
  Cardinality: 0           # 索引的基数估计值
     Sub_part: NULL        #
       Packed: NULL
         Null:
   Index_type: BTREE       # 索引类型， BTREE, FULLTEXT, HASH等
      Comment:
Index_comment:
      Visible: YES
   Expression: NULL
1 row in set (0.01 sec)

ERROR:
No query specified

# 查看表结构
mysql> desc user;
+---------+-------------+------+-----+---------+-------+
| Field   | Type        | Null | Key | Default | Extra |
+---------+-------------+------+-----+---------+-------+
| id      | int         | YES  |     | NULL    |       |
| name    | varchar(20) | YES  |     | NULL    |       |
| cert_no | varchar(20) | NO   | PRI |         |       |
+---------+-------------+------+-----+---------+-------+
3 rows in set (0.00 sec)
```

为 `user.cert_no` 添加了唯一性约束后，可以看到 `mysql` 自动为 `user` 表添加了 `cert_no` 索引。

通过查看表结构 (`desc user`) 可以发现，`user.cert_no` 的 `key` 显示为 `pri` 主键，这是 `MySQL` 的一个特殊情况：当表中没有显式定义主键，且存在一个 `NOT NULL` 的唯一索引时，`MySQL` 会将该唯一索引的列视为候选主键，并在 `DESC` 命令中将其 `Key` 标记为 `PRI`;

```sql
# 为 user.id 字段添加唯一约束
mysql> alter table user modify id int unique not null default 0;
Query OK, 0 rows affected (0.06 sec)
Records: 0  Duplicates: 0  Warnings: 0
# 查看索引信息
mysql> show index from user\G;
*************************** 1. row ***************************
        Table: user
   Non_unique: 0
     Key_name: cert_no
 Seq_in_index: 1
  Column_name: cert_no
    Collation: A
  Cardinality: 0
     Sub_part: NULL
       Packed: NULL
         Null:
   Index_type: BTREE
      Comment:
Index_comment:
      Visible: YES
   Expression: NULL
*************************** 2. row ***************************
        Table: user
   Non_unique: 0           # id 属性有唯一性约束，创建的索引也是唯一索引
     Key_name: id
 Seq_in_index: 1
  Column_name: id
    Collation: A
  Cardinality: 0
     Sub_part: NULL
       Packed: NULL
         Null:
   Index_type: BTREE
      Comment:
Index_comment:
      Visible: YES
   Expression: NULL
2 rows in set (0.00 sec)

ERROR:
No query specified

# 查看表结构，发现 id 属性的 key 为 UNI
mysql> desc user;
+---------+-------------+------+-----+---------+-------+
| Field   | Type        | Null | Key | Default | Extra |
+---------+-------------+------+-----+---------+-------+
| id      | int         | NO   | UNI | 0       |       |
| name    | varchar(20) | YES  |     | NULL    |       |
| cert_no | varchar(20) | NO   | PRI |         |       |
+---------+-------------+------+-----+---------+-------+
3 rows in set (0.00 sec)

mysql>
```


## 4.1、添加唯一性约束 (建表时)

```sql
# 默认约束名称与列名相同
create table 表名称(
字段名  数据类型,
字段名  数据类型  unique,  
字段名  数据类型  unique key,
字段名  数据类型
);

# 可以指定约束名称
create table 表名称(
字段名  数据类型,
字段名  数据类型,  
字段名  数据类型,
[constraint 约束名] unique key(字段名)
);
```

### 4.1.1、唯一性约束 (单列约束)

创建`room` 表，设置 `room.no`、`room.name` 两列单独设置唯一约束

```sql
mysql> create table if not exists room (id int primary key auto_increment, no int unique, name varchar(20) unique key, area double);
Query OK, 0 rows affected (0.09 sec)

mysql> desc room;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| no    | int         | YES  | UNI | NULL    |                |
| name  | varchar(20) | YES  | UNI | NULL    |                |
| area  | double      | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
4 rows in set (0.00 sec)

mysql> select * from information_schema.table_constraints where table_schema = 'test_constraints_1' and table_name = 'room';
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA  | CONSTRAINT_NAME | TABLE_SCHEMA       | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| def                | test_constraints_1 | name            | test_constraints_1 | room       | UNIQUE          | YES      |
| def                | test_constraints_1 | no              | test_constraints_1 | room       | UNIQUE          | YES      |
| def                | test_constraints_1 | PRIMARY         | test_constraints_1 | room       | PRIMARY KEY     | YES      |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
3 rows in set (0.00 sec)

mysql>
```

唯一性约束字段插入数据

```sql
# 唯一性约束数据插入
mysql> insert into room values(1, 1, 'room1', 100);
Query OK, 1 row affected (0.02 sec)
# 唯一性约束的字段可以插入 null 值
mysql> insert into room values(2, null, 'room2', 100);
Query OK, 1 row affected (0.01 sec)
# 唯一性约束的字段可以插入多个 null 值
mysql> insert into room values(3, null, 'room3', 100);
Query OK, 1 row affected (0.01 sec)
# 唯一性约束的字段插入重复的非空数据报错
mysql> insert into room values(4, null, 'room3', 100);
ERROR 1062 (23000): Duplicate entry 'room3' for key 'room.name'
mysql>

# 插入成功三条数据，null 值可重复
mysql> select * from room;
+----+------+-------+------+
| id | no   | name  | area |
+----+------+-------+------+
|  1 |    1 | room1 |  100 |
|  2 | NULL | room2 |  100 |
|  3 | NULL | room3 |  100 |
+----+------+-------+------+
3 rows in set (0.00 sec)

mysql>
```

### 4.1.2、唯一性约束 (复合约束)

创建`room1` 表，设置 `room.no + room.name` 两列组合的唯一约束。

```sql
# 字段列表中写的是多个字段名，多个字段名用逗号分隔，表示那么是复合唯一，即多个字段的组合是唯一的
mysql> create table if not exists room1 (
    id int primary key auto_increment, 
    no int, 
    name varchar(20), 
    area double, 
    constraint no_name unique key (no, name)
    );
Query OK, 0 rows affected (0.09 sec)

mysql> desc room1;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| no    | int         | YES  | MUL | NULL    |                |
| name  | varchar(20) | YES  |     | NULL    |                |
| area  | double      | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
4 rows in set (0.00 sec)

mysql> select * from information_schema.table_constraints where table_schema = 'test_constraints_1' and table_name = 'room1';
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA  | CONSTRAINT_NAME | TABLE_SCHEMA       | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| def                | test_constraints_1 | no_name         | test_constraints_1 | room1      | UNIQUE          | YES      |
| def                | test_constraints_1 | PRIMARY         | test_constraints_1 | room1      | PRIMARY KEY     | YES      |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
2 rows in set (0.01 sec)

mysql>
```


## 4.2、添加唯一性约束 (建表后)

```sql
# 字段列表中如果是一个字段，表示该列的值唯一。如果是两个或更多个字段，那么复合唯一，即多个字段的组合是唯一的
# 方式1：
alter table 表名称 add unique key(字段列表); 
# 方式2：
alter table 表名称 modify 字段名 字段类型 unique
```

> 创建 `room2` 表，结构如下

```sql
mysql> create table if not exists room2(id int primary key auto_increment, no int, name varchar(20), area double);
Query OK, 0 rows affected (0.07 sec)

mysql> desc room2;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| no    | int         | YES  |     | NULL    |                |
| name  | varchar(20) | YES  |     | NULL    |                |
| area  | double      | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
4 rows in set (0.01 sec)

mysql>
```


### 4.2.1、添加唯一性约束 (单列约束)

```sql
# 给 no 字段添加唯一性约束
mysql> alter table room2 add unique key (no);
Query OK, 0 rows affected (0.05 sec)
Records: 0  Duplicates: 0  Warnings: 0

mysql> desc room2;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| no    | int         | YES  | UNI | NULL    |                |
| name  | varchar(20) | YES  |     | NULL    |                |
| area  | double      | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
4 rows in set (0.00 sec)
# 给 name 字段添加唯一性约束
mysql> alter table room2 modify name varchar(20) unique;
Query OK, 0 rows affected (0.05 sec)
Records: 0  Duplicates: 0  Warnings: 0

mysql> desc room2;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| no    | int         | YES  | UNI | NULL    |                |
| name  | varchar(20) | YES  | UNI | NULL    |                |
| area  | double      | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
4 rows in set (0.00 sec)

# 查看约束ALTER TABLE USER 
ADD UNIQUE(NAME,PASSWORD);
ALTER TABLE USER 
ADD CONSTRAINT uk_name_pwd UNIQUE(NAME,PASSWORD);
ALTER TABLE USER 
MODIFY NAME VARCHAR(20) UNIQUE;
mysql> select * from information_schema.table_constraints where table_schema = 'test_constraints_1' and table_name = 'room2';
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA  | CONSTRAINT_NAME | TABLE_SCHEMA       | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| def                | test_constraints_1 | name            | test_constraints_1 | room2      | UNIQUE          | YES      |
| def                | test_constraints_1 | no              | test_constraints_1 | room2      | UNIQUE          | YES      |
| def                | test_constraints_1 | PRIMARY         | test_constraints_1 | room2      | PRIMARY KEY     | YES      |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
3 rows in set (0.01 sec)
```

### 4.2.2、添加唯一性约束 (复合约束)

```sql
# 添加复合唯一性约束
ALTER TABLE USER ADD UNIQUE(NAME,PASSWORD);

# 符合唯一性约束如果需要指定约束名称，通过 [constraint 约束名称] 的方式指定约束的名称
ALTER TABLE USER ADD CONSTRAINT uk_name_pwd UNIQUE(NAME,PASSWORD);

ALTER TABLE USER MODIFY NAME VARCHAR(20) UNIQUE;
```

```shell
# 添加 no_name 复合约束
mysql> alter table room2 add constraint no_name unique(no, name);
Query OK, 0 rows affected (0.06 sec)
Records: 0  Duplicates: 0  Warnings: 0

mysql> desc room2;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| no    | int         | YES  | UNI | NULL    |                |
| name  | varchar(20) | YES  | UNI | NULL    |                |
| area  | double      | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
4 rows in set (0.00 sec)

mysql> select * from information_schema.table_constraints where table_schema = 'test_constraints_1' and table_name = 'room2';
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA  | CONSTRAINT_NAME | TABLE_SCHEMA       | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
| def                | test_constraints_1 | name            | test_constraints_1 | room2      | UNIQUE          | YES      |
| def                | test_constraints_1 | no              | test_constraints_1 | room2      | UNIQUE          | YES      |
| def                | test_constraints_1 | no_name         | test_constraints_1 | room2      | UNIQUE          | YES      |
| def                | test_constraints_1 | PRIMARY         | test_constraints_1 | room2      | PRIMARY KEY     | YES      |
+--------------------+--------------------+-----------------+--------------------+------------+-----------------+----------+
4 rows in set (0.00 sec)

mysql>
```

### 4.2.3、删除唯一性约束

- 添加唯一性约束的列上也会自动创建唯一索引。
- 删除唯一约束只能通过删除唯一索引的方式删除。
- 删除时需要指定唯一索引名，唯一索引名就和唯一约束名一样。
- 如果创建唯一约束时未指定名称，如果是单列，就默认和列名相同；如果是组合列，那么默认和`()`中排在第一个的列名相同。也可以自定义唯一性约束名。


```sql
# 查看都有哪些约束
SELECT * FROM information_schema.table_constraints WHERE table_name = '表名';

# 删除索引，从而删除列的唯一约束
ALTER TABLE USER DROP INDEX uk_name_pwd;
```

注意：可以通过 `show index from 表名称;` 查看表的索引

```sql
# 查看 room2 表的约束信息
mysql> select * from information_schema.table_constraints where table_schema='testdb' and table_name = 'room2';
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA | CONSTRAINT_NAME | TABLE_SCHEMA | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| def                | testdb            | name            | testdb       | room2      | UNIQUE          | YES      |
| def                | testdb            | no              | testdb       | room2      | UNIQUE          | YES      |
| def                | testdb            | no_name         | testdb       | room2      | UNIQUE          | YES      |
| def                | testdb            | PRIMARY         | testdb       | room2      | PRIMARY KEY     | YES      |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
4 rows in set (0.00 sec)
```

> 删除符合索引 user.no_name，从而删除 符合唯一约束

```shell
# 删除 no_name 索引
mysql> alter table room2 drop index no_name;
Query OK, 0 rows affected (0.01 sec)
Records: 0  Duplicates: 0  Warnings: 0

# 再次查询 user 表的约束信息，发信 no_name 没有了
mysql> select * from information_schema.table_constraints where table_schema='testdb' and table_name = 'room2';
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA | CONSTRAINT_NAME | TABLE_SCHEMA | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| def                | testdb            | name            | testdb       | room2      | UNIQUE          | YES      |
| def                | testdb            | no              | testdb       | room2      | UNIQUE          | YES      |
| def                | testdb            | PRIMARY         | testdb       | room2      | PRIMARY KEY     | YES      |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
3 rows in set (0.00 sec)
```



# 5、主键约束 `PRIMARY KEY`

主键（`PRIMARY KEY`）约束是数据库中用于唯一标识表中每一行记录的约束规则。它兼具 `NOT NULL` 和 `UNIQUE` 的特性，且一张表只能有一个主键。

- 一个表最多只能有一个主键约束，建立主键约束可以在列级别创建，也可以在表级别上创建。
- 主键约束对应着表中的一列或者多列（复合主键）；
- 如果是多列组合的复合主键约束，那么这些列都不允许为空值，并且组合的值不允许重复。
- `MySQL` 的主键名总是 `PRIMARY`，就算自己命名了主键约束名也没用。
- 当创建主键约束时，系统默认会在所在的列或列组合上建立对应的主键索引（能够根据主键查询的，就根据主键查询，效率更高）。如果删除主键约束了，主键约束对应的索引就自动删除了。
- 需要注意的一点是，不要修改主键字段的值。因为主键是数据记录的唯一标识，如果修改了主键的值，就有可能会破坏数据的完整性。


## 5.1、添加主键约束 (建表时)

```shell
# #列级模式
create table 表名称(
字段名 数据类型 primary key,
字段名 数据类型,
字段名 数据类型
);

# #表级模式
create table 表名称(
字段名 数据类型,
字段名 数据类型,
字段名 数据类型,
[constraint 约束名] primary key(字段名)
);
```

> 建表时，使用列级约束设置主键约束 (单列主键约束)

```sql
# 建表时，使用列级约束设置主键约束
mysql> create table if not exists user1(id int primary key, name varchar(20), cert_no varchar(20));
Query OK, 0 rows affected (0.02 sec)

# 查看 user1 的表结构
mysql> desc user1;
+---------+-------------+------+-----+---------+-------+
| Field   | Type        | Null | Key | Default | Extra |
+---------+-------------+------+-----+---------+-------+
| id      | int         | NO   | PRI | NULL    |       |
| name    | varchar(20) | YES  |     | NULL    |       |
| cert_no | varchar(20) | YES  |     | NULL    |       |
+---------+-------------+------+-----+---------+-------+
3 rows in set (0.00 sec)

# 查看 user1 表的约束信息，只有一个 名为 PRIMARY 的主键约束
mysql> select * from information_schema.table_constraints where table_name = 'user1';
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA | CONSTRAINT_NAME | TABLE_SCHEMA | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| def                | testdb            | PRIMARY         | testdb       | user1      | PRIMARY KEY     | YES      |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
1 row in set (0.00 sec)

mysql> show index from user1\G;
*************************** 1. row ***************************
        Table: user1
   Non_unique: 0
     Key_name: PRIMARY
 Seq_in_index: 1
  Column_name: id
    Collation: A
  Cardinality: 0
     Sub_part: NULL
       Packed: NULL
         Null:
   Index_type: BTREE
      Comment:
Index_comment:
      Visible: YES
   Expression: NULL
1 row in set (0.01 sec)

ERROR:
No query specified

mysql>
```

查看表 `user1` 的索引信息，可以发现 `mysql` 自动为 `user1` 表创建了一个名为 `PRIMARY` 的唯一非空索引，索引绑定的列为 `id`;

> 建表时，使用表级约束设置主键约束 (符合主键约束)

```sql
# 设置 id + cert_no 的符合主键约束，命名为 idx_id_cert_no
mysql> create table if not exists user2(id int, name varchar(20), cert_no varchar(20), constraint idx_id_cert_no primary key(id, cert_no));
Query OK, 0 rows affected (0.02 sec)

# 查看表结构，可以看到 id 和 cert_no 都设置了非空，且都是 pri
mysql> desc user2;
+---------+-------------+------+-----+---------+-------+
| Field   | Type        | Null | Key | Default | Extra |
+---------+-------------+------+-----+---------+-------+
| id      | int         | NO   | PRI | NULL    |       |
| name    | varchar(20) | YES  |     | NULL    |       |
| cert_no | varchar(20) | NO   | PRI | NULL    |       |
+---------+-------------+------+-----+---------+-------+
3 rows in set (0.00 sec)

# 查看 user2 的约束信息
mysql> select * from information_schema.table_constraints where table_name = 'user2';
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| CONSTRAINT_CATALOG | CONSTRAINT_SCHEMA | CONSTRAINT_NAME | TABLE_SCHEMA | TABLE_NAME | CONSTRAINT_TYPE | ENFORCED |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
| def                | testdb            | PRIMARY         | testdb       | user2      | PRIMARY KEY     | YES      |
+--------------------+-------------------+-----------------+--------------+------------+-----------------+----------+
1 row in set (0.00 sec)
```

创建复合主键约束时，会默认设置每一列非空且每一列都会设置一个唯一非空的索引；

```sql
# user2 设置了符合的主键约束 idx_id_cert_no
mysql> show index from user2\G;
# id 列创建了一个唯一索引
*************************** 1. row ***************************
        Table: user2
   Non_unique: 0
     Key_name: PRIMARY
 Seq_in_index: 1
  Column_name: id
    Collation: A
  Cardinality: 0
     Sub_part: NULL
       Packed: NULL
         Null:
   Index_type: BTREE
      Comment:
Index_comment:
      Visible: YES
   Expression: NULL
# cert_no 列也创建了一个唯一索引
*************************** 2. row ***************************
        Table: user2
   Non_unique: 0
     Key_name: PRIMARY
 Seq_in_index: 2
  Column_name: cert_no
    Collation: A
  Cardinality: 0
     Sub_part: NULL
       Packed: NULL
         Null:
   Index_type: BTREE
      Comment:
Index_comment:
      Visible: YES
   Expression: NULL
2 rows in set (0.00 sec)

ERROR:
No query specified

mysql>
```

## 5.2、删除主键约束

删除主键约束的语法如下:

```sql
alter table 表名称 drop primary key;
``

- 删除主键约束不需要指定主键名称，以为一张表只能有一个主键;
- 删除主键约束后，对应列的唯一约束同步删除；
- 删除主键约束后，对应列的索引同步删除;
- 山竹主键约束后，对应列的非空约束仍然保留;

> 删除 `user2` 表的主键约束

```sql
mysql> alter table user2 drop primary key;
Query OK, 0 rows affected (0.04 sec)
Records: 0  Duplicates: 0  Warnings: 0

# 查看表结构信息，此时 id 和 cert_no 列不再具有唯一约束
mysql> desc user2;
+---------+-------------+------+-----+---------+-------+
| Field   | Type        | Null | Key | Default | Extra |
+---------+-------------+------+-----+---------+-------+
| id      | int         | NO   |     | NULL    |       |
| name    | varchar(20) | YES  |     | NULL    |       |
| cert_no | varchar(20) | NO   |     | NULL    |       |
+---------+-------------+------+-----+---------+-------+
3 rows in set (0.00 sec)

# 查看 user2 表的索引信息，此时索引已经清空了
mysql> show index from user2\G;
Empty set (0.00 sec)

ERROR:
No query specified
```

# 6、自增列 `AUTO_INCREMENT`

`AUTO_INCREMENT​` 是 `MySQL` 中用于自动生成唯一递增整数值的属性。它通常应用于整数类型的主键列，以便在插入新行时无需手动指定该列的值，由数据库自动分配。

1. 一个表最多只能有一个自增长列；
2. 当需要产生唯一标识符或顺序值时，可设置自增长；
3. 自增长列约束的列必须是键列（主键列，唯一键列）；
4. 自增约束的列的数据类型必须是整数类型；
5. 如果自增列指定了 `0 `和 `null`，会在当前最大值的基础上自增；如果自增列手动指定了具体值，直接赋值为具体值。

使用如下命令可以查看自增属性的下一次插入使用的值是多少:

```sql
SHOW TABLE STATUS LIKE 'your_table_name'\G
```

> 查看 `user3` 表自增属性的下一次值是多少

```sql
mysql> show table status like 'user3'\G;
*************************** 1. row ***************************
           Name: user3
         Engine: InnoDB
        Version: 10
     Row_format: Dynamic
           Rows: 4
 Avg_row_length: 4096
    Data_length: 16384
Max_data_length: 0
   Index_length: 16384
      Data_free: 0
 Auto_increment: 6                        # 自增属性的下一次自增数字是 6
    Create_time: 2026-09-08 00:14:12
    Update_time: 2026-09-08 00:18:25
     Check_time: NULL
      Collation: utf8mb4_0900_ai_ci
       Checksum: NULL
 Create_options:
        Comment:
1 row in set (0.01 sec)

ERROR:
No query specified

mysql>
```

## 6.1、设置自增约束 (建表时)

建表时，通过如下方式为对应列添加自增属性

```sql
# 给主键约束的列添加自增属性
create table 表名称(
字段名 数据类型 primary key auto_increment,
字段名 数据类型 unique key not null,
字段名 数据类型 unique key,
字段名 数据类型 not null default 默认值,
);

# 给唯一约束的列添加自增属性
create table 表名称(
字段名 数据类型 default 默认值 ,
字段名 数据类型 unique key auto_increment,
字段名 数据类型 not null default 默认值,,
primary key(字段名)
);
```

> 创建 user3 表，设置 id 为 主键 & 自增

```sql
mysql> create table if not exists user3(id int primary key auto_increment, name varchar(20), cert_no varchar(20) unique);
Query OK, 0 rows affected (0.04 sec)

mysql> desc user3;
+---------+-------------+------+-----+---------+----------------+
| Field   | Type        | Null | Key | Default | Extra          |
+---------+-------------+------+-----+---------+----------------+
| id      | int         | NO   | PRI | NULL    | auto_increment |
| name    | varchar(20) | YES  |     | NULL    |                |
| cert_no | varchar(20) | YES  | UNI | NULL    |                |
+---------+-------------+------+-----+---------+----------------+
3 rows in set (0.00 sec)

mysql>
```

> 向 `user3` 表插入数据，自增字段传入 `null` 或 `0` 此时系统会自动计算属性值;

```sql
mysql> insert into user3 values(null, 'zhangsan', '10001');
Query OK, 1 row affected (0.01 sec)

mysql> insert into user3 values(0, 'lisi', '10002');
Query OK, 1 row affected (0.00 sec)

mysql> select * from user3;
+----+----------+---------+
| id | name     | cert_no |
+----+----------+---------+
|  1 | zhangsan | 10001   |
|  2 | lisi     | 10002   |
+----+----------+---------+
2 rows in set (0.00 sec)

mysql>
```

> 向 `user3` 表插入数据，自增字段传入自定义的值

```sql
# 自动给 id 赋值，此时id会使用自定义的值
mysql> insert into user3 values(5, 'wangwu', '10005');
Query OK, 1 row affected (0.01 sec)

mysql> select * from user3;
+----+----------+---------+
| id | name     | cert_no |
+----+----------+---------+
|  1 | zhangsan | 10001   |
|  2 | lisi     | 10002   |
|  5 | wangwu   | 10005   |
+----+----------+---------+
3 rows in set (0.00 sec)

# 如果传入的 id 是一个已经存在的值，则此时会报异常
mysql> insert into user3 values(1, 'xiaoming', '10006');
ERROR 1062 (23000): Duplicate entry '1' for key 'user3.PRIMARY'

# 向 user3 中插入一个非最新 & 未被使用的 id 时，数据可以正常插入
mysql> insert into user3 values(4, 'xiaoming', '10004');
Query OK, 1 row affected (0.01 sec)

# 可以看到 xiaoming 数据插入成功 id=4
mysql> select * from user3;
+----+----------+---------+
| id | name     | cert_no |
+----+----------+---------+
|  1 | zhangsan | 10001   |
|  2 | lisi     | 10002   |
|  4 | xiaoming | 10004   |
|  5 | wangwu   | 10005   |
+----+----------+---------+
4 rows in set (0.00 sec)

# 查看 user3 表下一次自增的值是多少
mysql> show table status like 'user3'\G;
*************************** 1. row ***************************
           Name: user3
         Engine: InnoDB
        Version: 10
     Row_format: Dynamic
           Rows: 4
 Avg_row_length: 4096
    Data_length: 16384
Max_data_length: 0
   Index_length: 16384
      Data_free: 0
 Auto_increment: 6                        # 下一次自增的值是 6
    Create_time: 2026-09-08 00:14:12
    Update_time: 2026-09-08 00:18:25
     Check_time: NULL
      Collation: utf8mb4_0900_ai_ci
       Checksum: NULL
 Create_options:
        Comment:
1 row in set (0.01 sec)

ERROR:
No query specified

mysql>
```

## 6.2、设置自增约束 (建表后)

建表后使用如下命令添加自增约束

```sql
alter table 表名称 modify 字段名 数据类型 auto_increment;
```

> 创建 user4 表，建表后给 id 添加自增属性

```sql
mysql> create table if not exists user4(id int primary key, name varchar(20), cert_no varchar(20) unique);
Query OK, 0 rows affected (0.04 sec)

mysql> desc user4;
+---------+-------------+------+-----+---------+-------+
| Field   | Type        | Null | Key | Default | Extra |
+---------+-------------+------+-----+---------+-------+
| id      | int         | NO   | PRI | NULL    |       |
| name    | varchar(20) | YES  |     | NULL    |       |
| cert_no | varchar(20) | YES  | UNI | NULL    |       |
+---------+-------------+------+-----+---------+-------+
3 rows in set (0.00 sec)

# 为 id 列添加自增属性，添加自增属性时 无需带上 primary key 主键约束信息
mysql> alter table user4 modify id int auto_increment;
Query OK, 0 rows affected (0.05 sec)
Records: 0  Duplicates: 0  Warnings: 0

mysql> desc user4;
+---------+-------------+------+-----+---------+----------------+
| Field   | Type        | Null | Key | Default | Extra          |
+---------+-------------+------+-----+---------+----------------+
| id      | int         | NO   | PRI | NULL    | auto_increment |
| name    | varchar(20) | YES  |     | NULL    |                |
| cert_no | varchar(20) | YES  | UNI | NULL    |                |
+---------+-------------+------+-----+---------+----------------+
3 rows in set (0.00 sec)
```

## 6.3、删除自增约束


自增属性可以通过如下的方式删除

```sql
# 给这个字段增加自增约束
# alter table 表名称 modify 字段名 数据类型 auto_increment;

# 去掉auto_increment相当于删除
alter table 表名称 modify 字段名 数据类型;
```

> 删除 user4 表的 自增属性

```sql
# 修改 id 列信息，不带上自增属性即表示删除自增属性
mysql> alter table user4 modify id int;
Query OK, 0 rows affected (0.06 sec)
Records: 0  Duplicates: 0  Warnings: 0

# 查看 user4 的表结构信息，发现自增属性已经没了
mysql> desc user4;
+---------+-------------+------+-----+---------+-------+
| Field   | Type        | Null | Key | Default | Extra |
+---------+-------------+------+-----+---------+-------+
| id      | int         | NO   | PRI | NULL    |       |
| name    | varchar(20) | YES  |     | NULL    |       |
| cert_no | varchar(20) | YES  | UNI | NULL    |       |
+---------+-------------+------+-----+---------+-------+
3 rows in set (0.00 sec)

mysql>
```

## 6.4、`MySql8.0`新特性 - 自增变量的持久化

在 `MySQL 8.0` 之前，自增主键 `AUTO_INCREMENT` 的值如果大于 `max(primary key)+1`，在 `MySQL` 重启后，会重置 `AUTO_INCREMENT=max(primary key)+1` ，这种现象在某些情况下会导致业务主键冲突或者其他难以发现的问题。

 下面通过案例来对比不同的版本中自增变量是否持久化。 在 `MySQL 5.7` 版本中，测试步骤如下：创建的数据表中包含自增主键的 `id` 字段，语句如下：


```sql
# 创建 user5 表
mysql> create table if not exists user5(id int primary key auto_increment, name varchar(20));
Query OK, 0 rows affected (0.02 sec)

mysql> desc user5;
+-------+-------------+------+-----+---------+----------------+
| Field | Type        | Null | Key | Default | Extra          |
+-------+-------------+------+-----+---------+----------------+
| id    | int         | NO   | PRI | NULL    | auto_increment |
| name  | varchar(20) | YES  |     | NULL    |                |
+-------+-------------+------+-----+---------+----------------+
2 rows in set (0.00 sec)

# 连续插入三条数据
mysql> insert into user5 values(null, 'zhangsan');
Query OK, 1 row affected (0.01 sec)

mysql> insert into user5 values(null, 'lisi');
Query OK, 1 row affected (0.01 sec)

mysql> insert into user5 values(null, 'wangwu');
Query OK, 1 row affected (0.03 sec)

# 查询 user5 信息，id数字递增
mysql> select * from user5;
+----+----------+
| id | name     |
+----+----------+
|  1 | zhangsan |
|  2 | lisi     |
|  3 | wangwu   |
+----+----------+
3 rows in set (0.00 sec)

# =================================

# 删除 id=3 的数据
mysql> delete from user5 where id = 3;
Query OK, 1 row affected (0.01 sec)

mysql> select * from user5;
+----+----------+
| id | name     |
+----+----------+
|  1 | zhangsan |
|  2 | lisi     |
+----+----------+
2 rows in set (0.00 sec)

# ================================

# 再次插入数据，使用 id 自增
mysql> insert into user5 values(0, 'xiaoming');
Query OK, 1 row affected (0.00 sec)

# 可以看到新增的自增主键数字为 4
mysql> select * from user5;
+----+----------+
| id | name     |
+----+----------+
|  1 | zhangsan |
|  2 | lisi     |
|  4 | xiaoming |
+----+----------+
3 rows in set (0.00 sec)
```


